server:
  port: 8001

spring:
  application:
    name: cloud-provider-hystrix-payment

eureka:
  client:
    register-with-eureka: true  #注册到eureka
    fetch-registry: true  #自己是服务，需要从eureka注册中心获取已注册的列表
    service-url:
      defaultZone: http://eureka7001.com:7001/eureka/
